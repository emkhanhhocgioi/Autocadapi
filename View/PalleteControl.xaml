<UserControl x:Class="test.View.PalleteControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:test.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <Grid Background="DarkCyan">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" 
                   Text="Custom pallate" 
                   FontSize="14" 
                   FontWeight="Bold" 
                   Foreground="White" 
                   Margin="10" 
                   HorizontalAlignment="Center"/>

        <Button Grid.Row="1" Grid.ColumnSpan="2" 
                Command="{Binding DrawLineCM}" 
                Content="Line"/>

        <Button Grid.Row="2" Grid.ColumnSpan="2" 
                Command="{Binding DrawCircle}" 
                Content="Circle"/>

        <Button Grid.Row="3" Grid.Column="0" 
                Content="Rectangle"
                Command="{Binding DrawRectOnMouse}"/>

        <Grid Grid.Row="3" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" 
                       Text="W:" 
                       VerticalAlignment="Center" 
                       Foreground="White" 
                       Margin="5,0"/>

            <TextBox Grid.Column="1" 
                     Width="50" 
                     Height="25" 
                     Text="{Binding RectW, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,0,5,0"/>

            <TextBlock Grid.Column="2" 
                       Text="H:" 
                       VerticalAlignment="Center" 
                       Foreground="White" 
                       Margin="5,0"/>

            <TextBox Grid.Column="3" 
                     Width="50" 
                     Height="25" 
                     Text="{Binding RectH, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>

        <Button Grid.Row="4" Grid.ColumnSpan="2" 
                Content="RESET"
                Command="{Binding ResetWH}"/>

    </Grid>
</UserControl>